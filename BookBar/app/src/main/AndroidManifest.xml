<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssdut411.app.bookbar" >

    <application
        android:allowBackup="true"
        android:name=".activity.system.MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/DayTheme" >
        <activity
            android:name=".activity.system.MainActivity"
            android:windowSoftInputMode="adjustPan|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.person.LoginActivity"/>
        <activity android:name=".activity.person.RegisterActivity"/>
        <activity android:name=".activity.person.ForgetPasswordActivity"/>
    </application>

</manifest>
